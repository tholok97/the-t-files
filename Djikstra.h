#ifndef DJIKSTRAHEADER
#define DJIKSTRAHEADER

#include <iostream>
#include <vecctor>
#include <fstream>

class Djikstra {
	private:
		
	public:
		Djikstra(vector<vector<int>> vec, int start, int end, 
				bool edgeList = false);
}

Djikstra::Djikstra(vector<vector<int>> matrix, int start, int end, 
		bool edgeList = false) {
	
}

#endif